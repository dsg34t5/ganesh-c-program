#include <iostream> 
using namespace std; 
 
int main()
{ 
    int x,y,k; 
    cout<<"Enter  x number"; 
    cin>>x; 
    cout<<"Enter y number"; 
    cin>>y; 
    cout<<"enter k number";
    cin>>k;
    for(int i=x+1;i<y;i++) 
    { 
        cout<<i<<" "; 
        if(i==k||i==k)
        k++;
    } 
    return 0; 
} 
